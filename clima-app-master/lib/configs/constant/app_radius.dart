import 'package:flutter/rendering.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppRadius {
  AppRadius._();

  static BorderRadius r10 = const BorderRadius.all(Radius.circular(10)).r;
  static BorderRadius r18 = const BorderRadius.all(Radius.circular(18)).r;
  static BorderRadius r20 = const BorderRadius.all(Radius.circular(20)).r;
  static BorderRadius r100 = const BorderRadius.all(Radius.circular(100)).r;
}
